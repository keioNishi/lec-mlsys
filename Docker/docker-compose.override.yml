# GPU configuration (automatically generated by run.sh)
services:
  colab:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
